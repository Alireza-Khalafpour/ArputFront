'use client'

import "../styles/GeneralLoader.css"

const GeneralLoader = () => {
    return (
        <div className="w-full text-center m-auto items-center justify-center flex">
            <span class="loader2"></span>
        </div>
    );
}

export default GeneralLoader;